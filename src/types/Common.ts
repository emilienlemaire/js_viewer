export interface Size {
  width: number;
  height: number;
}

export interface Position {
  x: number | null;
  y: number | null;
}

export interface ZoomInfo {
  x: number;
  y: number;
  k: number;
}
